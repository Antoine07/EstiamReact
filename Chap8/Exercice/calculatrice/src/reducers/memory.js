
const stateInit = {
    memory: []
}

// reducer <=> Algorithmique de votre store
export default (state = stateInit, action = {}) => {

    switch (action.type) {

       


        default:
            return state;
    }
}